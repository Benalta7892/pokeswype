<div class="container">
  <div class="banner">

    <div class="pikachu-logo"></div>
  </div>
    <div class="title-home">
      <div class="yellow-star"></div>
      <p class="text-ul"><span>Favorites</span></p>
    </div>
  <div class="a-home"><% @cards.sample(3).each do |card| %>
      <%= link_to card_path(card) do %>
      <%= image_tag(card.image, class: "cards-pokemon") %>
      <% end %>
      <% end %>
  </div>
</div>
