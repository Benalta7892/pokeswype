<%# déplacer le formulaire de recherche dans le layout modal navbar
http://localhost:3000/cards/search?query=pikatchu %>

<div class="container-search">
  <div class="card-profil-search">
    <div class="a-home"><% if @cards.any? %>
      <% @cards.each do |card| %>
      <%= link_to image_tag(card.image) %>
          <% end %>
          <% else %>
      <em>Sorry, no cards found.</em>
      <% end %>
    </div>
  </div>
</div>
