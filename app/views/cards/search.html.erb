<%# déplacer le formulaire de recherche dans le layout modal navbar
http://localhost:3000/cards/search?query=pikatchu %>

<div class="container-search">
  <%= link_to root_path, class: "arrow" do %>
    <%= image_tag asset_path('back-arrow.png') %>
  <% end %>
  <div class="card-profil-search">
    <div class="a-home">
      <% if @cards.any? %>
        <% @cards.each do |card| %>
          <%= link_to card_path(card) do %>
            <%= image_tag(card.image) %>
            <p><%= card.ready_for_exchange_collectors.count %>are ready for an exchange</p>
          <% end %>
        <% end %>
      <% else %>
        <h1><em>Sorry, no cards found.</em><h1>
      <% end %>
    </div>
  </div>
</div>
